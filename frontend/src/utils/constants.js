// Dynamically set API_BASE_URL to use the same origin as the frontend
export const API_BASE_URL = `${window.location.origin}/api`;
// For local development:
// export const API_BASE_URL = "http://localhost:8000/api";
