# Rulenix frontend

React 19, Redux Toolkit, Vite, and Tailwind CSS client for Rulenix.

```powershell
npm install
npm run dev
```

The development server runs on `http://localhost:5173` and proxies `/api` to
the Rust service at `http://localhost:8080`.
